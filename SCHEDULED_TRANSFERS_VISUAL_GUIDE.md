# Scheduled Transfers & Templates - Visual Guide

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     Tallow Transfer System                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐       ┌──────────────────────────┐
│   React Components       │       │   Plain TS Modules       │
├──────────────────────────┤       ├──────────────────────────┤
│                          │       │                          │
│ ScheduleTransferDialog   │◄──────┤ scheduled-transfer.ts    │
│ TransferTemplates        │       │ transfer-templates.ts    │
│ ScheduledTransfersPanel  │       │                          │
│                          │       └────────────┬─────────────┘
└──────────┬───────────────┘                    │
           │                                    │
           │                                    │
           ▼                                    ▼
┌──────────────────────────┐       ┌──────────────────────────┐
│   Zustand Stores         │       │   localStorage           │
├──────────────────────────┤       ├──────────────────────────┤
│                          │       │                          │
│ useTransferStore         │       │ tallow-scheduled-*       │
│ useDeviceStore           │       │ tallow-transfer-*        │
│                          │       │                          │
└──────────────────────────┘       └──────────────────────────┘
```

## Scheduled Transfer Flow

```
User Action
    │
    ▼
┌─────────────────────────┐
│  Select Files           │
│  Select Device          │
│  Set Time & Repeat      │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  scheduleTransfer()     │
│  - Validate inputs      │
│  - Create schedule obj  │
│  - Save to localStorage │
│  - Schedule timer       │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Timer Execution        │
│  - Check device online  │
│  - Retry if unavailable │
│  - Execute transfer     │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Transfer Complete      │
│  - Update status        │
│  - Calculate next run   │
│  - Notify listeners     │
└─────────────────────────┘
```

## Template Application Flow

```
User Action
    │
    ▼
┌─────────────────────────┐
│  Browse Templates       │
│  - Default templates    │
│  - Custom templates     │
│  - Usage stats          │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Select Template        │
│  "Secure Transfer"      │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  applyTemplate()        │
│  - Get template options │
│  - Update use count     │
│  - Return settings      │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Configure Transfer     │
│  - Apply encryption     │
│  - Apply compression    │
│  - Apply other settings │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────┐
│  Execute or Schedule    │
└─────────────────────────┘
```

## Component Interaction

```
┌───────────────────────────────────────────────────────────────┐
│                      User Interface                            │
└───────────────────────────────────────────────────────────────┘

┌─────────────────────┐    ┌─────────────────────┐
│                     │    │                     │
│  TransferTemplates  │    │  ScheduleTransfer   │
│                     │    │     Dialog          │
│  ┌───────────────┐  │    │                     │
│  │ Quick Transfer│  │    │  ┌───────────────┐  │
│  │ Secure        │  │    │  │ Files: 3      │  │
│  │ Private       │  │    │  │ Device: Mac   │  │
│  │ [Create New]  │  │    │  │ Time: 10:00   │  │
│  └───────────────┘  │    │  │ Repeat: Daily │  │
│                     │    │  └───────────────┘  │
│  [Apply Template]   │    │                     │
│                     │    │  [Schedule]         │
└──────────┬──────────┘    └──────────┬──────────┘
           │                          │
           └──────────┬───────────────┘
                      │
                      ▼
           ┌──────────────────────┐
           │                      │
           │ ScheduledTransfers   │
           │      Panel           │
           │                      │
           │  Active (2)          │
           │  ┌────────────────┐  │
           │  │ Daily Report   │  │
           │  │ Next: 10:00    │  │
           │  │ [Cancel]       │  │
           │  └────────────────┘  │
           │                      │
           │  History (5)         │
           │  ┌────────────────┐  │
           │  │ Backup         │  │
           │  │ Status: Done   │  │
           │  │ [Delete]       │  │
           │  └────────────────┘  │
           │                      │
           └──────────────────────┘
```

## Data Flow Diagram

```
┌──────────────┐
│   User UI    │
└──────┬───────┘
       │
       │ scheduleTransfer(options)
       │
       ▼
┌──────────────────────────────────────────────┐
│  scheduled-transfer.ts                       │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ 1. Generate UUID                       │ │
│  │ 2. Convert Files to FileInfo           │ │
│  │ 3. Create ScheduledTransfer object     │ │
│  │ 4. Add to scheduledTransfers array     │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ saveScheduledTransfers()               │ │
│  │   localStorage.setItem(...)            │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ scheduleTimer()                        │ │
│  │   setTimeout(executeTransfer, delay)   │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │ notifyListeners()                      │ │
│  │   listeners.forEach(fn => fn())        │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
       │
       │ Transfer scheduled: ID
       │
       ▼
┌──────────────┐
│   User UI    │
│  (Updated)   │
└──────────────┘
```

## State Management Pattern

```
┌─────────────────────────────────────────────────────────┐
│               Plain TypeScript Module                    │
│                                                          │
│  // CORRECT: Use .getState() for non-reactive access   │
│                                                          │
│  function checkDevice(deviceId: string) {               │
│    const store = useDeviceStore.getState();            │
│    const device = store.getDeviceById(deviceId);       │
│    return device?.isOnline ?? false;                   │
│  }                                                      │
│                                                          │
│  // INCORRECT: Don't use hooks in plain modules        │
│  function badExample() {                               │
│    const device = useDeviceStore(                      │
│      state => state.getDeviceById(id)                 │
│    ); // This creates a reactive subscription!         │
│  }                                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│               React Component                            │
│                                                          │
│  // CORRECT: Use hooks in React components             │
│                                                          │
│  function MyComponent() {                              │
│    const device = useDeviceStore(                      │
│      state => state.selectedDevice                     │
│    );                                                   │
│                                                          │
│    return <div>{device?.name}</div>;                   │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
```

## Timer Management

```
Schedule Created
      │
      ▼
┌───────────────────────┐
│ Calculate Delay       │
│ delay = target - now  │
└──────────┬────────────┘
           │
           ▼
┌───────────────────────┐
│ setTimeout(fn, delay) │
│ Store timer ID        │
└──────────┬────────────┘
           │
           │  On Cancel
           │  ┌─────────────────┐
           │  │ clearTimeout()  │
           │  │ Remove timer ID │
           │  └─────────────────┘
           │
           ▼  Time Reached
┌───────────────────────┐
│ Execute Transfer      │
└──────────┬────────────┘
           │
           ▼
┌───────────────────────┐
│ If Repeat != 'once'   │
│ Calculate next run    │
│ Schedule new timer    │
└───────────────────────┘
```

## Template System

```
┌─────────────────────────────────────────────────────┐
│                   Template Store                     │
├─────────────────────────────────────────────────────┤
│                                                      │
│  Default Templates (Built-in)                       │
│  ┌────────────────────────────────────────────────┐ │
│  │ • Quick Transfer   - Fast & Simple            │ │
│  │ • Secure Transfer  - PQC + Compression        │ │
│  │ • Private Transfer - PQC + Onion + Metadata   │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  Custom Templates (User Created)                    │
│  ┌────────────────────────────────────────────────┐ │
│  │ • Work Files       - Specific device          │ │
│  │ • Family Photos    - Max privacy              │ │
│  │ • Quick Share      - No encryption            │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  Template Options                                   │
│  ┌────────────────────────────────────────────────┐ │
│  │ deviceId?: string                             │ │
│  │ compression?: boolean                         │ │
│  │ encryption?: 'pqc' | 'standard' | 'none'     │ │
│  │ maxSize?: number                              │ │
│  │ autoAccept?: boolean                          │ │
│  │ stripMetadata?: boolean                       │ │
│  │ enableOnionRouting?: boolean                  │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  Usage Tracking                                     │
│  ┌────────────────────────────────────────────────┐ │
│  │ useCount: number                              │ │
│  │ lastUsed: timestamp                           │ │
│  └────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

## UI Component Tree

```
TransferPage
│
├── FileDropZone
│   └── (File selection)
│
├── DeviceDiscovery
│   └── (Device list)
│
├── TransferTemplates
│   ├── Stats Cards
│   │   ├── Total Templates
│   │   ├── Custom Templates
│   │   └── Most Used
│   │
│   ├── Template Grid
│   │   ├── TemplateCard (Default)
│   │   ├── TemplateCard (Custom)
│   │   └── TemplateCard (Custom)
│   │
│   └── TemplateModal (Create/Edit)
│       ├── Name Input
│       ├── Description Input
│       ├── Options Select
│       └── Action Buttons
│
├── ScheduledTransfersPanel
│   ├── Active Section
│   │   ├── ScheduledTransferCard
│   │   │   ├── Status Badge
│   │   │   ├── Device Info
│   │   │   ├── File Count
│   │   │   ├── Countdown Timer
│   │   │   └── Cancel Button
│   │   └── ...
│   │
│   └── History Section
│       └── ScheduledTransferCard
│           ├── Status Badge
│           ├── Completion Info
│           └── Delete Button
│
└── ScheduleTransferDialog (Modal)
    ├── File List Preview
    ├── Device Selector
    ├── DateTime Picker
    ├── Repeat Options
    │   ├── Once
    │   ├── Daily
    │   └── Weekly
    ├── Auto-retry Checkbox
    └── Action Buttons
        ├── Cancel
        └── Schedule
```

## Status Colors

```
Scheduled Transfers:
  ┌──────────────────────────────────────┐
  │ 🟣 scheduled    #8b5cf6 (Purple)    │
  │ 🔵 running      #3b82f6 (Blue)      │
  │ 🟢 completed    #10b981 (Green)     │
  │ 🔴 failed       #ef4444 (Red)       │
  │ ⚫ cancelled    #6b7280 (Gray)      │
  └──────────────────────────────────────┘

Templates:
  ┌──────────────────────────────────────┐
  │ 🟣 Custom       #8b5cf6 (Purple)    │
  │ 🔵 Default      #3b82f6 (Blue)      │
  │ 🟢 Enabled      #10b981 (Green)     │
  │ ⚫ Disabled     #6b7280 (Gray)      │
  └──────────────────────────────────────┘
```

## File Organization

```
Tallow/
│
├── lib/
│   └── transfer/
│       ├── transfer-manager.ts         (Existing)
│       ├── store-actions.ts            (Existing)
│       ├── scheduled-transfer.ts       ✨ NEW
│       └── transfer-templates.ts       ✨ NEW
│
├── components/
│   └── transfer/
│       ├── FileDropZone.tsx            (Existing)
│       ├── DeviceDiscovery.tsx         (Existing)
│       │
│       ├── ScheduleTransferDialog.tsx           ✨ NEW
│       ├── ScheduleTransferDialog.module.css    ✨ NEW
│       │
│       ├── TransferTemplates.tsx                ✨ NEW
│       ├── TransferTemplates.module.css         ✨ NEW
│       │
│       ├── ScheduledTransfersPanel.tsx          ✨ NEW
│       ├── ScheduledTransfersPanel.module.css   ✨ NEW
│       │
│       ├── TransferManagementExample.tsx        ✨ NEW
│       │
│       ├── SCHEDULED_TRANSFERS_AND_TEMPLATES_README.md  ✨ NEW
│       ├── SCHEDULED_TRANSFERS_QUICK_REF.md             ✨ NEW
│       │
│       └── index.ts                    (Updated)
│
└── SCHEDULED_TRANSFERS_IMPLEMENTATION_SUMMARY.md ✨ NEW
```

## Quick Start Flowchart

```
┌─────────────────┐
│  Start Here     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Import modules  │
│ and components  │
└────────┬────────┘
         │
         ▼
    ┌────────────────────┐
    │ What do you need?  │
    └─────┬─────┬────────┘
          │     │
    ┌─────┘     └─────┐
    │                 │
    ▼                 ▼
┌─────────────┐  ┌─────────────┐
│ Schedule    │  │ Template    │
│ Transfer    │  │ Management  │
└──────┬──────┘  └──────┬──────┘
       │                │
       ▼                ▼
┌─────────────┐  ┌─────────────┐
│ Use Dialog  │  │ Use Manager │
│ Component   │  │ Component   │
└──────┬──────┘  └──────┬──────┘
       │                │
       └────────┬───────┘
                │
                ▼
       ┌────────────────┐
       │ View Scheduled │
       │ Transfers      │
       └────────────────┘
```

## Success!

All components are now ready to use in the Tallow application. Follow the documentation and examples to integrate into your project.
