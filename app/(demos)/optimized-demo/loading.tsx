/**
 * Loading state for optimized demo page
 * Shows while the entire page is loading
 */

import { DashboardSkeleton } from '@/components/loading/page-skeleton';

export default function Loading() {
  return <DashboardSkeleton />;
}
